<%= form_with model: @booking do |booking_form| %>
    <%= booking_form.fields_for :passengers do |passenger_form| %>
        <%= passenger_form.label :name %>
        <%= passenger_form.text_field :name %><br />
        <%= passenger_form.label :email %>
        <%= passenger_form.text_field :email %><br /><br />
    <% end %>

    <%= booking_form.hidden_field :flight_id, value: params[:flight] %>

    <%= booking_form.submit "Book Flight" %>
<% end %>