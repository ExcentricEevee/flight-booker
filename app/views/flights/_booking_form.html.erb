<%= form_with url: "/bookings/new", model: @booking, method: :get do |flight_form| %>
    <% flight_form.hidden_field :passengers, value: params[:passengers] %>
    <%= flight_form.collection_radio_buttons :flight, @flights, :id,
        :departure_airport %>
<% end %>